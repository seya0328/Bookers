<h1>Books</h1>
<p></p>
<h2>New book</h2>


<% @books.each do |book| %>
 
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  
  <h4>本文</h4>
  <%= f.text_area :body %>
  
  <%= f.submit '投稿' %>
 
  <span>
    <%= link_to book.title, "/books/#{book.id}" %>
  </span>
<% end %>